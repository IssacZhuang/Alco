using System;
using Alco.Editor.ViewModels;
using Avalonia.Controls;

namespace Alco.Editor.Utility;

public static class ControlExtension
{
    // public static EditorEngine GetEngine(this Control control)
    // {
    //     TopLevel topLevel = TopLevel.GetTopLevel(control) ?? throw new InvalidOperationException("Failed to resolve engine: Control is not attached to a top level");
    //     return (topLevel.DataContext as ViewModels.Editor)!.Engine;
    // }
}

